# Twin Paradox Distortion — 仕様書

## 1. 製品概要

2つの「コンプレッサー＋ディストーション」ステージを入れ子（ネスト）構造で配置し、中間経路に配した2点のフィルターによって相互変調させることで、極めて低いCPU負荷でありながら演奏のニュアンスに応じて有機的・カオスに変貌するサウンドを生み出す超軽量ディストーション・プラグイン。

---

## 2. シグナル・ルーティング（内部構造）

```
[ Input ]
   │
   ├──► 【Stage 1 (外殻)】
   │       ├── [ Comp A ]
   │       └── [ Distortion A ] ◄──────────────────┐
   │                │                               │
   │                ▼                               │ [ Filter 2 (Feedback) ]
   │         [ Filter 1 (Forward) ]                 │ (Stage 2出力を帯域制限して
   │                │                               │  Stage 1の歪みへ変調FB)
   │                └───────────────────┐           │
   │                                   ▼           │
   └──► 【Stage 2 (内殻)】 ─────────────┴───────────┘
           ├── [ Comp B ]
           └── [ Distortion B ]
                    │
                    ▼
               [ Output ]
```

**変調ロジック:**
- Comp A のエンベロープ → Stage 2 の Drive B を乗算変調
- Filter 2 経由の Stage 2 出力 → Stage 1 の Drive A を乗算変調
- 乗算のみ（重い計算なし）→ CPU負荷は最小

---

## 3. パラメーター仕様（全6ノブ）

| パラメーター         | ID         | 範囲              | デフォルト | 説明 |
|---------------------|------------|-------------------|------------|------|
| Drive 1             | drive1     | 0.0 – 1.0         | 0.5        | 外殻ステージの歪み量。全体のキャラクターを決定。 |
| Drive 2             | drive2     | 0.0 – 1.0         | 0.5        | 内殻ステージの歪み量。カオス度・飽和感を決定。 |
| Filter 1 (Forward)  | filter1fc  | 200 – 8000 Hz     | 1000 Hz    | Stage 1→Stage 2 に送る周波数帯域の制限（表のフィルター）。 |
| Filter 2 (Feedback) | filter2fc  | 200 – 8000 Hz     | 1000 Hz    | Stage 2→Stage 1 へ戻す周波数帯域の制限（裏の絞り弁）。 |
| Mod Blend           | modblend   | 0.0 – 1.0         | 0.5        | 2ステージ間の相互変調量。0でステージ独立動作。 |
| Mix                 | mix        | 0.0 – 1.0         | 1.0        | Dry/Wet 最終バランス。 |

---

## 4. 簡易結果マップ（サウンド特性）

Filter 1（表）のカットオフ × Filter 2（裏）のカットオフ設定によるサウンド変化:

|                          | Filter 2: Low (低域FB)        | Filter 2: High (高域FB)             |
|--------------------------|-------------------------------|--------------------------------------|
| **Filter 1: Low (低域)** | **ワームホール・ベース** — 中域のジュワジュワした歪みの裏で低域が脈打つ。 | **アシッド・サイコ・ワウ** — ピッキングでワウが跳ね上がり高域が鋭く鳴く。 |
| **Filter 1: High (高域)**| **ローファイ・バグ・ラジオ** — 高域チリチリと低域コンプの息継ぎが混ざる。 | **モスキート・スクリーム** — 高域が入れ子でループ。強音時のみゲートが閉じる。 |

---

## 5. アルゴリズム設計（軽さの秘密）

- **非モデリング処理**: アナログ回路シミュレーションなし。tanh ソフトクリッピングを採用。
- **乗算変調**: カオスな挙動は Comp A/B エンベロープを相手の Drive に乗算するだけで実現。
- **1次 IIR フィルター**: 2点の中間フィルターに最軽量の 1-pole lowpass を採用。
- **フィードバック安全化**: Filter 2 がハウリングを阻止しつつ「壊れる寸前の悲鳴」だけを抽出。

---

## 6. ビルド環境

- **フレームワーク**: JUCE 8
- **言語**: C++17
- **ターゲット**: VST3 / Standalone (Windows)
- **ビルドシステム**: CMake 3.22+
